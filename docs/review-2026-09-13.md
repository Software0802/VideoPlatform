# 交接与复核清单 — 2026-09-13

日期：2026-09-13。基线：`main` @ `90f2cff`（已推送 origin）。
状态：本文所列「真实上游验收未做」一项已由 `docs/acceptance-2026-09-13.md` 取代（五条链路成片 + 可灵 30s 长片实证，暴露的六处代码问题已在 `545580f` 修复）。
用途：这一轮改动交给独立审查者（另一模型）复核用。第 3 节写清哪些是实测、哪些是推断，第 4 节是建议重点查的地方。项目当前状态以 `docs/handoff.md` 为准，本文不重复它。

## 1. 版本对照

| 位置 | 版本 | 说明 |
| --- | --- | --- |
| `origin/main` | `90f2cff` | 已推送 |
| 本地 `main` | `90f2cff` | 与 origin 一致，工作树干净 |
| 生产运行 | `98759a5` 的构建产物 | `90f2cff` 相对 `98759a5` 只改了 `docs/handoff.md`（4 增 4 删），**代码零差异**，故生产与 `main` 在代码层面等价 |

上一版生产是 `da0348c`（H 包）。再往前 `04efdce`（R01–R09 + A–D 包 + 资金迁移）。

## 2. 本轮改了什么

两个功能提交，一个文档提交，一个基线更新提交。

### 2.1 `4690767` 智能体上游失败错误码拆分

`src/lib/agent/run-turn.ts`、`run-turn.test.ts`、`src/lib/i18n/messages/{zh-CN,en}/common.ts`。

原来 502「LLM 调用失败、本轮费用已退回」与 503「实例没配对话 provider」共用 `agent_unavailable`，前端无法区分。现在 502 用 `agent_upstream_failed`（`turn.error.code` 同步），503 保持 `agent_unavailable`。字典加 `common.err.agent_upstream_failed`，前端走 H 包已有的 `errorText(t, e)` 按码翻译，视图无改动。

### 2.2 `f6b8c88` 画布 DAG 运行审查修复（7 项）

来源：一次只读审查（Devin swe-2-max，静态推演 + 画布单测）给出 11 条 findings，判定 7 条成立。Codex 复审补的 1 条也在内。

| 项 | 位置 | 改法 |
| --- | --- | --- |
| F1（资金，最重要） | `src/lib/canvas/run-store.ts` `runHeldFunds` | transfer 份额计占用的条件由「jobId 不在任务索引」改为「jobId 不在索引 **且** 该节点执行位非终态」 |
| F2 | `src/lib/canvas/dag.ts` sweep | 幂等接管挪到成交价校验之前 |
| F11 | 同上 | `quote.items` 缺该节点快照时 `failed`/`internal_error` 失败关闭，不提交 |
| F3 | `src/lib/canvas/dag.ts` `createCanvasRun` | `withAdmissionLock` 内对报价采纳的历史产物重跑 `jobOutputUsable`，失效 409 `quote_stale` |
| F5 | `src/lib/canvas/dag.ts` `decideCanvasRunApproval` | 已 `cancelRequestedAt` 的 run 拒绝审批，409 `invalid_state` |
| F8 | `src/lib/jobs/admission.ts` | `withAdmissionLock` 的 tail 挂 `globalThis.__lumenAdmissionLockTail` |
| F6 + Codex#1 | `src/components/genius/canvas/CanvasView.tsx` | 三处轮询补 `.catch`；载入 effect 依赖回退为 `[]`，`t` 走 ref |

F1 修的具体故障：run 仍 `running` 时，用户删除其中一个**已终态**子任务 → `deleteJobById` 移除索引项 → 该份额被重新计为 run 占用 → 可用余额被压低，直到 run 终态才释放。修复后语义：执行位终态 = 份额已结算/退回，不计；执行位非终态或缺失 + job 缺失 = 崩溃孤儿，照旧计占用（宁可多占不超卖）。

`src/lib/canvas/run-graph.test.ts` 追加 `describe("审查修复回归 2026-09-12")` 5 条（F1/F2/F3/F5/F11），该文件现 29 条。

### 2.3 `98759a5` / `90f2cff` 文档

`docs/handoff.md`、`docs/design.md`（§2h 智能体、§2j 画布、§12.3 配额）、`AGENTS.md`（后端约定的智能体段与画布段）改写成当前状态。

## 3. 已验证 / 未验证

### 3.1 实测通过（可复现）

在 `f6b8c88` 的树上跑的，命令与数字如下：

| 命令 | 结果 |
| --- | --- |
| `pnpm exec tsc --noEmit` | 0 错 |
| `pnpm exec eslint src` | 0 错 0 警告 |
| `pnpm test` | 99 文件、1154 通过、1 跳过、0 失败 |
| `E2E_PORT=3100 E2E_ISOLATED=1 pnpm e2e` | 33 通过、0 失败（2.5 分钟） |

e2e 必须带那两个环境变量：3000 端口常被主仓库自己的 `next dev` 占着，复用它会让 setup 注册失败、28 条用例不跑。

部署：`bash scripts/deploy.sh` 一次跑通（18M 包，两个 Turbopack 别名自动补软链），`systemctl is-active genius` = active，服务器本机 `/api/health` 200 `{"ok":true}`，未触发回滚。公网 `/login` 200、`/api/health` 200、`/` 307 跳登录。

注意：部署脚本末尾对 `/` 判定「非 200 即告警」，而未登录跳 `/login` 的 307 是正常行为，那行告警可以忽略（脚本判定偏严，未改）。

### 3.2 未验证 —— 请审查者重点当作「未知」

- **真实上游验收**：生产上没有跑过任何真任务。全程 mock + 健康检查，没有发起过付费调用。生成链路（kling / yman / openai-image）在这版代码下是否真的能出片，未知。
- **F1–F11 的生产行为**：全部修复只有单测覆盖，没有在生产制造过对应故障（删除子任务、崩溃窗口、取消与审批竞态）。绿门禁不等于专项验收。
- **并发语义**：`withAdmissionLock` 与 run 锁都是**进程内**语义。单实例假设下成立，多实例或管理 CLI 与线上服务并发改同一 `user.json` 时不成立（这是既有设计约束，非本轮引入）。
- **Codex 复审的覆盖面**：Codex 审了本轮 diff（`--mode uncommitted`，审的是重排前的同一批改动），只给出 2 条 findings，都在 UI effect 依赖与测试覆盖上，**对任务书点名的资金/锁序硬约束没有产出任何 finding**。这不等于那些点已被验证通过，只等于 Codex 没说话。

### 3.3 过程中的一次返工（影响可信度评估）

合并前发现 `main` 已被另一会话推进到 `f420a3f`（H 包，含「错误码本地化」，已部署）。本轮原本有一个同主题的包（服务端 error.code → 客户端多语言，含新建 `src/lib/client/errors.ts` + `describeError`），与 H 包的 `errorText` 是同一件事且 H 包更全、已上线，因此**整包丢弃**，只把智能体错误码拆分与画布修复在 `main` 之上重做。

复核时请注意：现在树上生效的是 H 包的 `src/lib/i18n/errorText.ts`，**不是** `src/lib/client/errors.ts`（后者已不存在于 `main`）。

## 4. 建议重点复核的位置

按优先级：

1. `src/lib/canvas/run-store.ts` `runHeldFunds`（约 233 行起）与 `listCanvasRunsStrict`（约 186 行）：新判定是否引入「执行位已终态、但份额实际尚未结算」的漏计窗口？`EXEC_SETTLED` 含 `"canceled"` 而执行位枚举无此值，是无害超集还是意图不清？
2. `src/lib/canvas/dag.ts` sweep：幂等接管前置后，是否可能把「不该接管的 job」接回来（例如 attempt 递增语义与 key 的关系）。
3. `src/lib/canvas/dag.ts` `createCanvasRun` 的锁内采纳复核：它防的是同一次调用里 `computeQuote` 之后的窄窗口；外部删除会先被 `quoteHash` 不匹配挡住。**现有回归用例 F3 走的是后者，不覆盖前者**（用例里有注释说明）。这段新代码实际上没有测试覆盖。
4. `src/lib/jobs/admission.ts` 的 `globalThis` 锁：与 `src/lib/canvas/run-store.ts` 的 run 锁写法是否一致，热重载下是否真的单例。
5. 锁序：run → admission → job → user 是否仍单向无环（`createCanvasRun` 在 admission 锁内不取 run 锁）。

## 5. 审查中未采纳的 4 条（需产品决策，不是遗漏）

| # | 位置 | 事实 | 未采纳的原因 |
| --- | --- | --- | --- |
| a | `src/components/genius/canvas/CanvasView.tsx:190` 起 `persist` 的 409 分支 | 遇 `revision_conflict` 时整份替换为服务端文档，冲突窗口内本地未落盘编辑丢失；注释自称「绝不静默覆盖」与实现不符 | 正确修法（保留本地副本 / 合并 UI）是产品设计，不是一行修补 |
| b | `src/lib/canvas/graph.ts:145` | `validateGraph` 校验所有 material 节点含未连线者，一个游离空素材节点会让整图报价 400 | 与计划文档字面一致，属 UX 取舍 |
| c | `src/lib/canvas/dag.ts:494`（`awaiting_approval`）、`:568`（`queue_full` 退避） | 两者都无超时，被遗弃的 run 会把剩余预留冻结到 run 终态 | 计划内取舍，加超时是产品决策 |
| d | `src/lib/billing/admission.ts:73` → `run-store.ts:186` | 每次准入都 strict 读该用户全部 run 文件；run 文件只增不删（无删除路由、无归档），IO 随时间线性增长 | 内测规模无感，属容量问题 |

## 6. 下一刀

按 `docs/handoff.md` §5。本轮之后新增的待办只有：上面 §5 的 4 条需要你拍板；真实上游验收需要一次预算授权。

## 7. 仓库之外的改动（不在本仓库，复核时会看不到）

本轮同时改了全局 skills 仓库 `D:\dev\repos\skills`（**未提交**），把子代理 coder / tester 的执行引擎换成 Devin CLI `swe-2-max`，跑在 Herdr pane 的交互模式里：`skills/agent-team/scripts/devin-run.mjs`（新增）、`agents/coder.md`、`agents/tester.md`、`README.md`、`CLAUDE.snippet.md`，以及全局 `~/.claude/CLAUDE.md` 的对应块。与本项目代码无关，但它是本轮 P1/P5/P6 三个包的实际产出方式，评估产出可信度时可能需要知道。
