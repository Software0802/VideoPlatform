# 后续计划 · 2026-09-13 起

状态：2026-09-13 起草，替代 `docs/plan-unimplemented-2026-09-08.md` §12 的切片表作为当前排期；E（Harness）已完成并上线，F/G/I/J 的正文方案仍以该文档为准，本文只定顺序、验收与待决。当前事实见 `docs/handoff.md`，as-built 见 `docs/design.md`。

## 0. 起点

- 生产 `545580f`：五条创作链路 + 可灵 30s 长片真实成片（`docs/acceptance-2026-09-13.md`）。
- 产品方向：多中转按能力路由，模型可配置、可供用户选择；Grok 为普通可选成员，生产无 key。
- 本轮验收暴露的六处代码问题已修（545580f）；三处上游漂移已在 `.env` 修正。

## 1. 需要用户拍板的（不拍板不实施）

| # | 事项 | 现状 | 备选 |
| --- | --- | --- | --- |
| D1 | **长片定价** | 30s 售 ¥12（`prices.video` 按 6×5s 推得），上游实付 $1.45≈¥10.4，毛利≈¥1.6；多一个角色再加 3 张图即近亏 | A. 长片单独定档（如 30/45/60 = ¥20/¥30/¥40）；B. 按「视频 + 图片张数」动态报价；C. 维持现价先观察 |
| D2 | `edit_video` / `extend_video` 去向 | 只有 grok 声明，生产 503 | A. 从路线图移出，UI 保持「即将上线」；B. 找中转承接（YMan 目录暂无编辑/延长模型） |
| D3 | 通用 OpenAI 兼容中转 provider | 每接一家中转要写一个 provider；YMan 生图已复用 OpenAI image 工厂 | A. 做「按 base URL + 模型表配置」的通用视频/图片 provider（N3）；B. 继续逐家手写 |
| D4 | G 支付网关选型 | 充值只能管理员发礼品码 / 直充 | 微信 / 支付宝 / Stripe（境外）——决定后按 `plan-unimplemented` §8 实施 |

## 2. 切片与顺序

### N1 · 收尾与补漏（不需拍板，先做）

| 项 | 内容 | 验收 |
| --- | --- | --- |
| N1.1 | 产品目录 `supportsLongForm`：可灵「标准」与 YMan「快速」声明为真（`catalog.ts`，对应测试 `catalog.test.ts:108` 反向断言改掉）；`ShellContext` 逻辑不动。**当前生产后端开了长片，但选中产品时前端时长芯片没有 30/45/60** | 登录生产，选「标准」能看到 30/45/60 并提交 |
| N1.2 | YMan 长片（档 B：r2v 参考图）真实验收一条 30s（≈¥3） | 成片、`harnessPlan` 含 `r2v` 镜、参考图为角色表 |
| N1.3 | 画布 DAG 经 UI 走真实上游（文生图 → 图生视频复用，≈¥2.5） | run 成功、预留转移与结算对账 |
| N1.4 | `minimax-h3` 真实积分核对（YMan 后台账单或 `/v1/models` 的 credits 字段），登记进 `yman/catalog.ts` | `costUsdActual` 不再用兜底估价 |
| N1.5 | 在 `545580f` 上跑一次全量 e2e（上次全量在 `f6b8c88`） | 33+ 通过 |
| N1.6 | 长片 UI：创作页任务卡按 `job.shots` 显示「生成分镜 n/m」已有；补角色表 / 首帧缩略图展示（可选） | 手测 |

### N2 · 长片商业化（依赖 D1）

- 定价实现（`prices.ts` 长片档 + 订阅页 / 报价文案 + 测试）。
- `costOverTarget` / 硬上限的靶线随新估价校准；`evals/` 增长片质量评分用例并记一次真实评分（`evals/rubric.md`）。
- 视觉 QC 阈值校准：用 `HARNESS_QC_VISUAL_MODEL`（带图模型）跑对照集后固定 `HARNESS_QC_VISUAL_THRESHOLD`。

### N3 · 供应商可扩展（依赖 D3）

- 通用 OpenAI 兼容中转 provider：视频走 `/videos`（YMan 形状）、图片走 `/images/generations|edits`，按 env JSON 配 `{id, baseUrlEnv, keyEnv, models: {...}, capabilities}`；产品目录从配置生成。
- 模型目录动态化：启动时拉 `GET /v1/models`（有 credits 字段的中转直接读价目），下架模型自动从可选列表剔除并告警（现在只告警不剔除）。
- `edit_video` / `extend_video` 按 D2 决定。

### N4 · F 其他视频模式 UI（`plan-unimplemented` §7）

- 「参考」「首尾帧」已露出；剩 `edit_video` / `extend_video` 的界面，依赖 D2。

### N5 · G 支付网关（依赖 D4，`plan-unimplemented` §8）

- Order 状态机、回调验签、`ref:pay:<orderId>` 资金入口、退款 unknown 态。

### N6 · I 运维（`plan-unimplemented` §10）

- 告警落地到实际渠道（`notifyAlert` 目前只写日志/文件——确认后接 webhook）；备份验证演练；`data/` 增长与 run 文件归档；内存 1.8G 下的并发上限实测（`HARNESS_SHOT_CONCURRENCY`）。

### J · 暂缓（挑战赛、技能市场、协作）不排期。

## 3. 每片的通用门禁

`pnpm exec tsc --noEmit`、`pnpm exec eslint src`、`pnpm exec vitest run` 全绿；改 UI 再跑 `pnpm e2e`；涉及生产的改动先只读探查、备份 `.env`、部署后公网验证业务（`deploy.sh` 已含健康检查与回滚）；付费调用先报价再执行，单次验收预算显式写明。

## 4. 已知风险

- 中转随时下架模型（本轮三处）：N3 的动态目录是根治，之前靠 `upstream_model_missing` 告警 + runbook 手工处理。
- 长片毛利薄（D1）。
- 生产单机 1.8G 内存：长片并发默认 2，多任务同时跑长片时注意 `free -m`。
