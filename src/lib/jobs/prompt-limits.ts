/**
 * 提示词长度上限，服务端与浏览器共用一份（review 2026-09-15 U-11）。
 *
 * 这个文件必须保持可被客户端 import：没有 `node:` 依赖、不读环境变量、不引 zod。
 * 面板原来只有一个 `maxLength={2000}` 的静默截断——粘 5600 字进去只留 2000，没有计数器、
 * 没有提示，成片不对也找不到原因；而音频页那个计数器写的是 `x/10000`，与服务端 schema
 * 的 2000 直接矛盾。两边各写一份数的结果就是这样。
 */

/** 服务端 `createJobBodySchema` 与面板 textarea 共用的硬上限。 */
export const PROMPT_MAX_LEN = 2000;

/** 计数器开始示警的位置：还剩一成时变色，别等截断了才知道。 */
export const PROMPT_WARN_LEN = 1800;
