# 上游由多家供应商按能力协作：各家 key 配好之后，谁被选中由下方
# 「视频路由 / 文生图路由」两节的 *_PROVIDER_ORDER 决定——配了 key 却没写进
# ORDER 的 provider 不会被选中；一把 key 都没有才走 mock（ffmpeg 水印片）。
# 建议先读「视频路由：能力 + 优先级」一节再回来配各家的 key。
#
# ── 可灵（Kling）直连视频 ───────────────────────────────────────────────
# key 缺失时可灵完全不参与路由；详细变量见下方「可灵」一节。
# KLING_API_KEY=
#
# ── OpenAI 兼容生图（官方或 ccgoai 这类中转）────────────────────────────
# **只影响文生图（text_to_image）**，视频与其他模式一律不读这几项。
# 设置 OPENAI_API_KEY 后文生图走 https://api.openai.com/v1/images/generations（gpt-image-1）；
# 是否被选中由 IMAGE_PROVIDER_ORDER 决定（默认 openai,grok，见下方路由一节）。
# 默认（官方路径）：画幅芯片映射为官方三档尺寸 + 本地居中裁切；1k/2k 芯片映射为 low/high 画质档。
# OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_IMAGE_MODEL=gpt-image-1

# 接 ccgoai 这类「兼容 OpenAI Images API 但支持任意尺寸」的中转时设为 1：
# 七个画幅全部按原生尺寸出图、零裁切（宽高都是 16 的倍数），1k/2k 芯片决定尺寸档：
#   1:1 1024x1024 / 2048x2048    16:9 1024x576 / 2048x1152    9:16 576x1024 / 1152x2048
#   4:3 1024x768  / 2048x1536    3:4  768x1024 / 1536x2048    3:2  1008x672 / 2016x1344
#   2:3 672x1008  / 1344x2016
# 官方 gpt-image-1 只认三档尺寸，接官方时**不要**打开。
# OPENAI_IMAGE_FLEXIBLE_SIZES=1
# OPENAI_IMAGE_MODEL=gpt-image-2
# OPENAI_BASE_URL=https://ccgoai.club/v1

# 画质档 low / medium / high / auto，默认 high。请求里不显式带 quality 时上游按 medium 计费，
# 所以本项目每次都显式传。只在 OPENAI_IMAGE_FLEXIBLE_SIZES=1 时生效（官方路径仍是 1k→low / 2k→high）。
# ⚠️ ccgoai 对 gpt-image-2 的 high 一律回 503 service_busy（确定拒单，可重试）——该中转要用 medium。
# OPENAI_IMAGE_QUALITY=high
#
# 设为 1 时文生图带参考图（referenceImages）会改走 POST /images/edits（multipart），
# provider 随之声明 supportsImageReference——harness 的三视图角色表与每镜首帧依赖它。
# 默认关：关着时带参考图的请求在本地被 400 拒绝。ccgoai 已实测透传（生产已开），YMan 未验证。
# OPENAI_IMAGE_EDITS_ENABLED=1

# 图片价目表（quality × 尺寸档 × 张数，不按 token）。尺寸档按最长边：≤1024=1K、≤2048=2K、更大=4K。
# 设置后按档计价并忽略 usage token 精算；不设置则沿用官方口径（output_tokens × $40/M）。
# JSON 坏掉会记一条 warn 并自动回落到 token 口径，不会影响出图。
# ⚠️ 单位随上游而定：ccgoai 扣的是**人民币额度**，不是美元。一旦配置本表，
#    job.json 里的 costUsdEstimate / costUsdActual 就是「上游额度」而非 USD，做配额时别当美元读。
# 下面是 2026-09 实测的 ccgoai gpt-image-2 价目（元/张，1K 与 2K 同价）：
# OPENAI_IMAGE_PRICE_TABLE={"low":{"1K":0.08,"2K":0.08,"4K":0.10},"medium":{"1K":0.13,"2K":0.13,"4K":0.15},"high":{"1K":0.20,"2K":0.20,"4K":0.23}}
# 注意：出图常需 30–120 秒。生图路径走自己的 OPENAI_IMAGE_TIMEOUT_MS / OPENAI_IMAGE_TASK_TIMEOUT_MS
# （见文件末尾），不吃 UPSTREAM_TIMEOUT_MS，且永不重发已计费的生成请求。

# ── 可灵（Kling）直连视频（docs/plan-kling-video.md）─────────────────────────
#
# 可灵开放平台「新系统」的单串 API Key（控制台生成，不是旧版 AK/SK）。
# 缺失时可灵完全不参与路由——是否在次序里都没用，key 是前提。
# KLING_API_KEY=
# 域名不带 /v1——接口路径本身就是 /text-to-video/<model>、/tasks。
# 国内版（klingai.com/dev，人民币）用 api-beijing；国际版（美元充值，$10=100 积分）
# 的 key 只在 api-singapore 有效，在 beijing 会回 1002「api key not found」。
# KLING_BASE_URL=https://api-beijing.klingai.com
# KLING_BASE_URL=https://api-singapore.klingai.com
#
# 走不走可灵由下面「视频路由：能力 + 优先级」一节的 VIDEO_PROVIDER_ORDER 决定，
# 默认 grok——只配一把 KLING_API_KEY 不会让可灵接管视频，必须显式把它写进次序里
# （或用旧开关 VIDEO_PROVIDER=kling）。
#
# 模型名同时是 URL 路径段。kling-2.5-turbo 请求形状相同，可直接切。
# KLING_VIDEO_MODEL=kling-2.6
#
# 720p | 1080p，**产品默认档**（2026-09-06 起）：用户在界面上选了分辨率就以用户为准，
# 这条只在他没选、且所选产品也没规定默认档时生效。480p 的请求向上归一到 720p。
# KLING_VIDEO_RESOLUTION=720p
#
# off | native。**有声只支持 1080p**，判定为有声时分辨率会被自动抬到 1080p 并写回任务记录
# （单价随之从 0.3 涨到 1.0 积分/秒）。同样降级为默认：用户选「无声」时以用户为准。
# 产品目录里的「高清有声」档要求这条是 native，否则它不会出现在 /api/models 里——
# 卖一个一定不出声的「有声」档比不卖更糟。
# KLING_VIDEO_AUDIO=off
#
# 积分 → USD 的换算率，只影响账目显示，默认 0.10（$10 = 100 积分的充值比例）。
# 提交时按「积分/秒 × 时长 × 本值」估价；实付以 poll 回来的 billing 为准（可灵是三家里
# 唯一给出真实扣费的上游）。
# KLING_USD_PER_UNIT=0.10
#
# 单个可灵任务的总时长上限（毫秒），与 runner 的 15 分钟轮询上限同口径。
# KLING_TASK_TIMEOUT_MS=900000
#
# ⚠️ 时长只有 5 / 10 两档（能力地图写的 3–10s 是营销口径，接口枚举只有这两个）。
#    服务端会把请求归一为「≤5 → 5、>5 → 10」并**写回 job.durationSec**——4 秒的请求
#    上游按 5 秒计费，账目必须如实。首页的时长芯片同时换成 5 / 10。

# ── YMan 中转渠道（视频，OpenAI 兼容）─────────────────────────────────────────
#
# https://vip.yman.cc/v1。建任务 POST /videos → 轮询 GET /videos/{id} →
# GET /videos/{id}/content 取 mp4（**取片也要带 Bearer**，不是匿名 CDN 直链）。
# 缺 key 时 yman 不参与路由，即便它出现在 VIDEO_PROVIDER_ORDER 里。
# YMAN_API_KEY=
# 带 /v1；缺 /v1 时会自动补上。
# YMAN_BASE_URL=https://vip.yman.cc/v1
#
# 文生视频 / 图生（参考生）视频各自的上游模型名。
# ⚠️ 必须用 `GET /v1/models` 返回的**展示名**，不要用后台的内部模型名——上游文档明确要求，
#    填内部名会 404。2026-09-13 实测 `/v1/models` 返回 21 个 id：
#      gpt-image-2 / Runway Gen-4 Turbo video (图生视频) / gpt-image-2.5-flare /
#      gpt-image-2.5-sunburst / sd2.0-MX / minimax-h3 768p / minimax_h3 / wan3.0-video /
#      sd-2.0-fast-真人 / grok-video-1.5 / minimax-h3-933-图文 / seedance2.0-fast满血 /
#      seedance2.0-不卡人脸 / seedance2.0-900-720p / minimax-h3 / gemini-3-pro-image-run /
#      nano-banana-2 / firefly-gpt-image-2 等
#    本项目登记了其中几个的档位与价目（见 catalog.ts），其余照样能填，只是按
#    YMAN_UNKNOWN_CREDITS 估价。旧的内部名（minimax-H3 文字 / minimax_h3_t2v /
#    minimax_h3_ref2v / grok-imagine-video-1.5-preview / seedance2.0-svip-900-720p /
#    seedance2.0 / sd2.5）仍被识别为别名并自动换成展示名，不必急着改配置。
# 默认这两个是同价档，区别只在收不收参考图：「minimax-h3」是纯文生（发参考图会被拒），
# 「minimax-h3-933-图文」收参考图（≤9 张）。
# YMAN_T2V_MODEL=minimax-h3
# YMAN_I2V_MODEL=minimax-h3-933-图文
#
# ⚠️ 画幅**不做归一**：登记的模型里只有「SD2.0 满血」出 1:1，其余只有 16:9 / 9:16。
#    首页画幅芯片按「ORDER 里所有有 key 的 provider 的并集」显示，1:1 的请求会自动交给
#    接得下的那一家（如可灵）；一家都接不下就 400，而不是把竖屏悄悄换成横屏。
#
# ⚠️ 计费按「积分 = 分辨率价 + 时长价」预扣（¥1 = 100 积分，失败自动退），**不与时长成正比**：
#    minimax_h3_* 的 5/10/15 秒是 40/90/140 积分，seedance2.0 的 10 秒与 15 秒同为 350。
#    服务端把请求时长**向上**取到该模型最近的一档（4→5、6/8→10、12→15）并写回
#    job.durationSec，首页时长芯片也跟着换成该模型的档位。
#    没有音频开关：出不出声由模型决定，所以「有声」芯片一律显示「无声 · 暂不可用」——
#    是「不可控」而不是「一定无声」，不可控的东西不向用户收有声的加价。
#
# 覆盖 / 追加模型能力与价目（上游随时上新模型，改这条比改代码快）。逐字段合并到内置表，
# 只想改价就只写 credits。坏 JSON 记一条 warn 后回落内置表。
# YMAN_MODEL_CATALOG={"my-new-model":{"durations":[5,10],"resolutions":["720p"],"ratios":["16:9","9:16"],"maxReferenceImages":9,"credits":{"resolution":{"720p":10},"duration":{"5":40,"10":90}}}}
#
# 目录里没有的模型（自己填的模型名）按多少积分估价。绝不为 0——costUsdEstimate 为 0
# 会让账目完全看不见这次调用。默认 150（¥1.5）。
# YMAN_UNKNOWN_CREDITS=150
#
# 单个 YMan 任务从提交到出片的**本地等待上限**（毫秒），默认 900000（15 分钟），上限 60 分钟。
# 与 KLING_TASK_TIMEOUT_MS 同一个用途：超时只是本地放弃等待，上游任务仍然活着、也仍然
# 已经计费，界面会明说这一点。中转渠道排队慢时把它调大，而不是让任务被判成失败。
# YMAN_TASK_TIMEOUT_MS=900000

# ── 可选：xAI（官方或 Sub2API 反代）─────────────────────────────────────
# grok provider 走 xAI REST，按秒计费。它只是 ORDER 里的普通成员（取值 grok），
# 不是必需项：目前只有它声明 edit_video / extend_video（依赖 xAI Files API），
# 没配 XAI key 时这两条模式无供应商承接（提交返 503 no_provider_available）。
# XAI_API_KEY=xai-...
# XAI_BASE_URL=https://api.x.ai/v1

# Sub2API 反代（拼车）。仅在未设置 XAI_API_KEY 时使用。
# SUB2API_API_KEY=sk-...
# 不填 XAI_BASE_URL 时默认 http://127.0.0.1:8080/v1

# ── 视频路由：能力 + 优先级（方案 §3.4「功能先于供应商」）───────────────────────
#
# 逗号分隔的优先级列表，取值 kling | yman | grok，**默认只有 grok**。
# 路由规则：按这个次序取第一个「配了 key **且** capabilities().modes 声明支持这个模式
# **且**没被判定积分耗尽」的 provider——所以它只表达偏好次序，不表达能力。编辑视频 /
# 延长视频目前只有 grok 声明支持（依赖 xAI Files API），30 / 45 / 60 秒长片按 i2v+t2v
# 能力走同一条 ORDER（可灵 / YMan 都能承接，HARNESS_ENABLED 开关控制）；这两类不需要在
# ORDER 里单独配置——ORDER 全没选中时，配了
# XAI key 且未耗尽才试 grok，否则配了任何真 key 就 503 no_provider_available，
# 完全没 key 才 mock。认不出的名字会被丢掉。
#
# 默认不含 kling / yman 是有意的：换供应商是一次显式选择。一把为了别的用途配上的
# KLING_API_KEY / YMAN_API_KEY 不该静默改变全站视频的落点、时长档位与成片质感。
# VIDEO_PROVIDER_ORDER=yman,kling,grok
#
# 旧的单一开关，仍兼容：未设 VIDEO_PROVIDER_ORDER 时 VIDEO_PROVIDER=kling 视为
# 「kling,grok」，=grok（以及任何非法值）视为只有「grok」，与默认次序一致。
# 两条都设时以 ORDER 为准。
# VIDEO_PROVIDER=grok
#
# 人民币 → 美元换算率（1 USD = 多少 CNY），只影响账目显示：YMan 按人民币面值的积分计费，
# 而 costUsdEstimate / costUsdActual 的口径是美元。默认 7.2，非法值与 0 回落默认。
# USD_CNY_RATE=7.2

# ── 文生图路由：能力 + 优先级 ────────────────────────────────────────────────
#
# 逗号分隔的优先级列表，取值 openai | yman | grok，默认 openai,grok——正是加 YMan 之前
# 那条硬编码阶梯（有 OPENAI_API_KEY 走 openai，否则 xAI，都没有才 mock），所以旧实例
# 不改配置行为不变。规则与 VIDEO_PROVIDER_ORDER 相同：取第一个「有 key 且声明支持
# text_to_image」的 provider。
# IMAGE_PROVIDER_ORDER=yman,openai,grok
#
# YMan 生图（与它的视频共用 YMAN_API_KEY / YMAN_BASE_URL，不必再配一把 key）。
# 它同样兼容 OpenAI Images API，任意尺寸 + 按「画质档 × 尺寸档」计费，所以这条通道
# 恒定按 flexible sizes + high 画质发，七个画幅全部原生出图、零裁切。
# YMAN_IMAGE_MODEL=gpt-image-2
#
# 设为 1 时 YMan 生图通道声明 supportsImageReference，参考图请求走 POST {YMAN_BASE_URL}/images/edits
# （multipart，字段形状与 OpenAI 一致）。默认关；YMan 是否支持 /images/edits 未验证。
# YMAN_IMAGE_EDITS_ENABLED=1
#
# YMan 生图的价目表，语义与上面的 OPENAI_IMAGE_PRICE_TABLE 完全一致（单位是人民币额度）。
# 两条通道各读各的表：同名的 gpt-image-2 在两家可能不是一个价。
# YMAN_IMAGE_PRICE_TABLE={"high":{"1K":0.20,"2K":0.20,"4K":0.23}}
#
# 超时没有单独的变量：这条通道复用 OpenAI 生图那两条 —— 单次 HTTP 用
# OPENAI_IMAGE_TIMEOUT_MS，202 异步出图的轮询总时限用 OPENAI_IMAGE_TASK_TIMEOUT_MS。
#
# 也可以反过来把 openai 那条指向 YMan（不走 IMAGE_PROVIDER_ORDER 的老办法，仍然有效）：
# OPENAI_API_KEY=<YMan 的 key>
# OPENAI_BASE_URL=https://vip.yman.cc/v1
# OPENAI_IMAGE_MODEL=gpt-image-2
# OPENAI_IMAGE_FLEXIBLE_SIZES=1

# ── 上游积分耗尽后的自动切换 ────────────────────────────────────────────────
#
# 提交时上游回「积分不足」（402 / 带额度码的 429 → quota_exhausted）时，服务端记下这家的
# 这条通道（视频 / 图片分开记，写在 data/provider-state.json），把这个任务改交给下一家接得下
# 的 provider，并在这段时间里绕开它。被拒的提交从来没有被计费，所以换家不是再买一次。
# 用户的报价（priceCny）只降不升：换家是我们内部的事，不能让用户多付；新家的档位更便宜就
# 照低的收。新家最短时长比用户选的还长、因而更贵时干脆不换，走原来的退避重试。
# 一家可用的都不剩时提交直接 503，绝不静默落 mock 交一段水印片冒充成片。
# /api/health 的 exhausted 字段能看到当前被绕开的是谁、到什么时候、上游原话是什么。
#
# 绕开多久（毫秒），默认 21600000（6 小时）。到点自动放回去重试一次——充值是常事，
# 而我们没有任何「余额恢复了」的主动信号。
# PROVIDER_EXHAUSTED_TTL_MS=21600000

# 强制走 mock（有 key 也出 ffmpeg 水印片，便于 UI）
# LUMEN_FORCE_MOCK=1

# ── 用户系统（docs/plan-users-quota.md）─────────────────────────────────────
#
# 会话 Cookie（lumen_session）的 HMAC 密钥。**必填**：未设置或短于 16 字符时
# 服务启动即失败，不做任何静默降级。轮换它 = 让所有人立刻登出。
# 生成（PowerShell）：
#   -join ((1..32) | ForEach-Object { '{0:x2}' -f (Get-Random -Max 256) })
# LUMEN_SESSION_SECRET=
#
# 管理员的 user id（不是邮箱——邮箱可被抢注）。未设置时没有任何人是管理员，
# 迁移前留下的「无主任务」（job.json 里没有 ownerId）便对所有人不可见。
# 设置后它唯一的特权是：自己的任务列表里同时能看到这些无主任务。
# 该 id 由下面的部署顺序第 2 步产生。
# LUMEN_ADMIN_USER_ID=
#
# 部署顺序（plan §4，避免出现「谁都能注册」或「谁都进不去」的窗口）：
#   1. 先在 .env 写好 LUMEN_SESSION_SECRET → 重启到新版本。
#      此时 data/invites/ 为空，没人能注册——这是安全的默认。
#   2. 管理员生成 1 个邀请码并注册自己：
#        node scripts/mint-invites.mjs 1 --note "管理员"
#      把注册接口返回的 userId 写进 LUMEN_ADMIN_USER_ID → 再重启一次。
#   3. 之后按需 `node scripts/mint-invites.mjs 10 --note "第一批内测"` 分发。
#      码只打印到标准输出，不写日志；--note 用来记录发给了谁，便于追溯。
#   4. 旧的 LUMEN_ACCESS_TOKEN 已完全退役（proxy 只认会话 Cookie），从 .env 删掉即可。
#
# 分享链接（POST /api/jobs/:id/share → /s/<token>）的有效期，单位小时，默认 24，
# 下限 1、上限 24×365。令牌用独立于 LUMEN_SESSION_SECRET 的派生密钥签名，
# 轮换 LUMEN_SESSION_SECRET 不会影响已签发的分享链接，反之亦然；没有主动吊销
# 机制，收回的唯一手段是等到期。
# SHARE_TTL_HOURS=24
#
# ── 产品目录（对用户露出的「模型」，src/lib/products/catalog.ts）──────────────
#
# 用户在界面上选的是**产品**（「标准」「高清有声」），提交时 POST /api/jobs 的 model 字段
# 传的是产品 id（video-standard），不是上游模型名——供应商与模型名不出现在浏览器里。
# GET /api/models 只列这一刻可用的产品（provider 配了 key、该通道未被判定积分耗尽）。
#
# 内置七档（不设这条就是它们）：
#   video-fast      「快速」    yman  · 720p · 16:9/9:16 · 5/10/15 秒 · 音轨不可控 · 参考图 9 张
#   video-standard  「标准」    kling · 720p/1080p · 16:9/9:16/1:1 · 5/10 秒 · 无声 · 支持首尾帧
#   video-hd-audio  「高清有声」 kling · 1080p · 5/10 秒 · 有声（需 KLING_VIDEO_AUDIO=native）
#   video-grok      「Grok」    grok  · 480p/720p/1080p · 七画幅 · 1–15 秒 · 有声 · 参考图 7 张
#   image-fast / image-standard / image-grok  · 1K/2K · 七画幅（yman / openai / grok）
#
# 覆盖 / 追加：数组，按 id 覆盖同名产品（逐字段合并，只想改默认档就只写 defaultResolution），
# 新 id 直接追加（必须自带 provider / model / modes）。坏 JSON 记一条 warn 后回落内置表。
# LUMEN_PRODUCTS=[{"id":"video-standard","name":"标准","defaultResolution":"1080p"},{"id":"video-pro","name":"专业","kind":"video","provider":"kling","model":"kling-2.5-turbo","modes":["text_to_video"],"resolutions":["1080p"],"aspectRatios":["16:9"],"durations":[5,10],"audio":"off","supportsLastFrame":false,"maxReferenceImages":0,"description":"更快的 1080p 档"}]
#
# ── relay 中转（data/relays.json 的种子，docs/design.md §2l）─────────────────
#
# 只在 data/relays.json 不存在时生效：首次启动把这段 JSON 解析写入文件，之后以文件
# 为准（改这里不会再生效，要用 /api/admin/relays 或直接改文件）。keyEnv 只写环境
# 变量名，key 值永远不进 relays.json。
# LUMEN_RELAYS=[{"id":"ccgoai","name":"CCGO","baseUrl":"https://ccgoai.club/v1","keyEnv":"CCGOAI_API_KEY","priority":10,"video":{"protocol":"openai-videos","defaults":{"text_to_video":"m-t2v","image_to_video":"m-i2v"}},"image":{"protocol":"openai-images","model":"gpt-image-2","quality":"medium","flexibleSizes":true},"catalog":{"source":"models-endpoint","models":{}}}]
# catalog.source=models-endpoint 的 relay 每隔多久重拉一次 /v1/models 刷新目录快照
# （毫秒，默认 1800000=30 分钟，下限 60000）；消失模型告警 + 对应产品自动隐藏。
# RELAY_CATALOG_REFRESH_MS=1800000
#
# ── 定价 × 余额（方案 2026-09 §3.2，主闸门）───────────────────────────────
#
# 用户售价表（人民币元）。不设就用内置默认表：
#   {"video":{"5":2,"10":4,"hd":1.5,"audio":1},"extend":3,"edit":4,"image":{"1k":0.5,"2k":1}}
# video 的 "5" / "10" 是 ≤5 秒 / 更长两档基价，hd 是 1080p 的倍率，audio 是出声的加价；
# 30 / 45 / 60 秒长片走 longForm 定档（默认 20 / 30 / 40 元，hd 倍率与 audio 加价照常叠加）。
# 可以只写要改的几项，其余走默认。
# 这是**对用户的售价**，与上面 OPENAI_IMAGE_PRICE_TABLE（我们付给上游的成本）无关；
# 坏 JSON 只记一条 warn 并回落默认表，不会挡住提交。
# LUMEN_PRICE_TABLE={"video":{"5":2,"10":4,"hd":1.5,"audio":1},"longForm":{"30":20,"45":30,"60":40},"extend":3,"edit":4,"image":{"1k":0.5,"2k":1}}
#
# 智能体（2026-09-06）：`agent.turn` 是**一轮对话**的售价，默认 ¥0.05。这一轮顺手创建的
# 生成任务不含在里面——那些照常各自按上面的档位计价、各自走 /api/jobs 的余额准入。
# 老的 LUMEN_PRICE_TABLE 里没有这一项也不用改：缺就回落默认。
# LUMEN_PRICE_TABLE={"agent":{"turn":0.05}}
#
# 准入判据是「余额 − 在途任务预留 ≥ 本次售价」，不够返回 402 insufficient_balance；
# 成功的任务在终态那一刻扣款并往 data/ledger/<userId>.jsonl 记一行，失败 / 取消 / 过期
# 只释放预留、不扣钱。充值走 `node scripts/grant-balance.mjs <邮箱> <金额> --note "..."`。
# 新账号注册即送 ¥5（SIGNUP_BONUS_CNY，src/lib/users/service.ts 里的常量，流水 ref:"signup"），
# 没有环境变量可以调这个数字，改动需要改代码。
#
# ── 每日配额（plan §6，只对文生图计数；余额模型上线后退居防滥用兜底）────────
#
# 每人每天能出的图数，默认 200（余额是主闸门，这条只挡脚本刷图）。口径是「今日成功 + 当前在途 < 上限」：
# 提交即预留一个名额，成功落盘转为永久占用；上游故障、超时、用户取消都会
# 让任务转终态并自动释放预留，**不占额度**。「今日」按北京时间自然日算，
# 0 点重置（不做每用户时区）。超限返回 429 quota_exceeded。
# 管理员不豁免配额；迁移前的无主任务不计入任何人的额度。
# 设为 0 = 暂停所有人的生图提交。
# FREE_DAILY_IMAGE_QUOTA=200
#
# 止损阀：今日「失败 + 取消」达到它就暂停该账号的新提交（429 failure_limit_reached），
# 默认 30。它独立于配额，挡的是「反复提交再失败」持续消耗上游余额。
# FREE_DAILY_FAILURE_LIMIT=30

# 数据目录（默认 ./data）
# DATA_DIR=

# ── 数据留存（plan §8）─────────────────────────────────────────────────────
#
# 终态任务（成功 / 失败 / 取消 / 过期）完成超过这么多天后，删掉它的
# data/jobs/<id>/inputs/ 与 outputs/，只在 job.json 写 artifactsPurgedAt——
# 状态不变，记录仍在，作品环显示「作品已过期清理」的占位卡，且不再允许一键重试。
# 清理挂在任务 runner 的每小时定时器上（启动跑一次）。
# data/tmp/ 与 data/idempotency/ 里超过 24 小时的文件同时清掉，与本项无关、不可关。
# 0 = 关闭清理，永久保留。默认 30。
# DATA_RETENTION_DAYS=30

JOB_CONCURRENCY=2
MAX_QUEUED_JOBS=20
UPSTREAM_TIMEOUT_MS=30000
UPSTREAM_RETRY_BASE_MS=250

# 单个账号能同时在途的任务数（方案 §3.2「安全收口」），默认 5。
# 上面的 MAX_QUEUED_JOBS 是全站承载力，这条是公平与防刷：没有它，一个账号可以把
# 20 个槽全占满，其他人只会看到「队列已满」，而余额那条闸门对此无能为力（他钱够）。
# 超限回 429 queue_full「你有 n 条任务进行中，请等待完成」。
# MAX_QUEUED_JOBS_PER_USER=5

# ── 运维告警（方案 §3.2「可观测性」）────────────────────────────────────────
#
# 配了就把这四类事件 POST 成一条 JSON 出去（企业微信 / 飞书 / Slack 的机器人地址都行）：
#   budget_exceeded    长片撞破预算上限，后续调用已被硬停
#   cost_over_target   实际成本超过提交预估 ×1.5（软线，任务照跑）
#   provider_exhausted 某家上游积分耗尽，任务已自动改走下一家——要去充值了
#   disk_low           DATA_DIR 所在卷剩余空间低于 5%，成片可能落不了盘
# 同一件事 10 分钟内只发一次。发送失败只记一条 warn，绝不影响主流程。
# 不设 = 不外发，这些事件仍然照常写日志。
# ALERT_WEBHOOK_URL=
# 单次外发超时（毫秒），默认 5000、上限 30000。挂住的 webhook 不能拖住任务。
# ALERT_WEBHOOK_TIMEOUT_MS=5000

# 上游轮询阶梯的上限（毫秒），默认 10000，范围 500–60000。
# 阶梯是：前 20 秒每 2 秒问一次（用户还看着），20→60 秒线性升到 5 秒，之后按这条上限。
# 调小 = 更快看到成片但更费上游配额与本机事件循环；调大反之。轮询仍然是真相，SSE 只是加速。
# UPSTREAM_POLL_MAX_MS=10000

# ── 智能体对话（2026-09-07 起用独立凭据）───────────────────────────────────
#
# 智能体每一轮用一次 OpenAI 兼容的 chat.completions。它有**自己的一把 key**，不再复用
# 生图那几把：OPENAI_BASE_URL 指的中转（ccgoai）与 YMAN 都是只出图的渠道，
# /chat/completions 分别回 503 与 400——拿它们说话的结果是每轮「扣款 → 失败 → 退款」。
#
# 顺序只有三档：
#   1) mock 实例（LUMEN_FORCE_MOCK=1，或一把上游 key 都没有）→ 本地确定性回复，不发请求
#   2) AGENT_API_KEY → 用它 + AGENT_BASE_URL（默认 https://api.openai.com/v1）
#   3) 否则 XAI_API_KEY（含 SUB2API_API_KEY）→ grok-4.6
# 两把都没有 = 智能体**不可用**：/api/agent/skills 下发 available:false（前端把输入卡
# 置灰），开会话与续一轮在扣款之前回 503 agent_unavailable。绝不静默落 mock 假装在工作。
# AGENT_API_KEY=
# 对话端点的 REST root，带 /v1（缺就补）。任何 OpenAI 兼容的对话中转都行。
# AGENT_BASE_URL=https://api.openai.com/v1
# 模型名默认 gpt-4o-mini（回落 xAI 那条默认 grok-4.6）；下面这一条是唯一的覆盖口，
# 不设即用默认，设了对两个提供方一律生效。
# AGENT_CHAT_MODEL=gpt-4o-mini

# 一致性管线（30 / 45 / 60 秒长片）。M2.4 起生效：开启后 UI 多出长片时长，
# 任务走 Director → Keyframe → Shots(+QC) → Stitch。mock 模式可放心开启；真实 key 下
# 单片成本 ≈ $2.1–4.2，超过预估 ×2 自动停止重试。
HARNESS_ENABLED=false
# 每个长片任务并行生成的 shot 数（1–4）
# HARNESS_SHOT_CONCURRENCY=2
# 视觉一致性 QC 阈值（0–1）。不设即跳过视觉打分——阈值需用 evals/runs 对照集校准后再固定。
# HARNESS_QC_VISUAL_THRESHOLD=0.6
# 视觉 QC 用的模型名。不设时用智能体对话模型（AGENT_CHAT_MODEL 或 agent 默认）；
# 对话走便宜小模型、而视觉打分要带图的大模型时用它覆盖。
# HARNESS_QC_VISUAL_MODEL=
# Harness 的 LLM 调用超时（Director 规划 / 视觉 QC），默认 120000（2 分钟），上限 5 分钟。
# Director 一次要产出整份长片计划，大模型实测常超过通用 UPSTREAM_TIMEOUT_MS 的 30s。
# HARNESS_LLM_TIMEOUT_MS=120000
# WORKFLOWS_ENABLED / JIMENG_ENABLED 已删除（2026-09-06 阶段一核实）：`grep -r` 全仓库
# 只有历史文档提到它们，代码里没有任何读取方——`docs/architecture.md` 是标了「已过时」的
# Phase 0 历史稿，Skills/Workflows（§8）与即梦首尾帧（M4）都还没实现到需要读开关的地步。

# 生图的两条超时，管的不是同一件事，别混：
#
# 1) OPENAI_IMAGE_TIMEOUT_MS —— **单次 HTTP 请求**的超时（毫秒），默认 180000，上限 10 分钟。
#    生成 POST 与取结果 GET 都用它。生成 POST 不重试：超时即失败，由用户决定是否重新提交
#    （每个被上游接受的生成请求都计费，自动重试就是重复付钱）。
# OPENAI_IMAGE_TIMEOUT_MS=180000
#
# 2) OPENAI_IMAGE_TASK_TIMEOUT_MS —— **整个异步出图任务**的总上限（毫秒），默认 600000（10 分钟），
#    上限 60 分钟。中转站（ccgoai）出图慢时不返回图片，而是回 HTTP 202 + 任务 id，本地按响应里的
#    poll_after_ms（默认 2000，下限 1s、上限 30s）轮询 GET /v1/images/tasks/{id}，成功后再
#    GET /v1/images/tasks/{id}/result 取二进制。实测 high 档 2K 一张约 102 秒，4K 更久，所以
#    这条要远宽于单请求超时。超时只是本地放弃等待：任务在上游仍活着，也仍然只在取回 result 时才计费。
# OPENAI_IMAGE_TASK_TIMEOUT_MS=600000

# smoke 脚本（scripts/smoke-lumen.mjs / smoke-cancel.mjs）用的登录账号。
# /api/* 全部需要会话，脚本先 POST /api/auth/login 拿 lumen_session Cookie 再跑用例。
# 账号不存在且给了 LUMEN_SMOKE_INVITE 时会先用该一次性邀请码注册。凭据只在本机环境，不进日志。
# LUMEN_SMOKE_EMAIL=
# LUMEN_SMOKE_PASSWORD=
# LUMEN_SMOKE_INVITE=
# 目标实例（默认 http://localhost:3000；Next 16 dev 对 127.0.0.1 回 403，别用它）
# LUMEN_URL=
