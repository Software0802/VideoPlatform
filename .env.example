# 官方 xAI（优先）。按秒计费。
# XAI_API_KEY=xai-...
# XAI_BASE_URL=https://api.x.ai/v1

# Sub2API 反代（拼车）。仅在未设置 XAI_API_KEY 时使用。
# SUB2API_API_KEY=sk-...
# 不填 XAI_BASE_URL 时默认 http://127.0.0.1:8080/v1

# OpenAI 官方生图。**只影响文生图（text_to_image）**，视频与其他模式一律不读这几项。
# 设置 OPENAI_API_KEY 后文生图走 https://api.openai.com/v1/images/generations（gpt-image-1）；
# 不设置则文生图仍按 XAI_API_KEY / SUB2API_API_KEY 走 xAI，两把 key 都没有才回落 mock。
# 默认（官方路径）：画幅芯片映射为官方三档尺寸 + 本地居中裁切；1k/2k 芯片映射为 low/high 画质档。
# OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_IMAGE_MODEL=gpt-image-1

# 接 ccgoai 这类「兼容 OpenAI Images API 但支持任意尺寸」的中转时设为 1：
# 七个画幅全部按原生尺寸出图、零裁切（宽高都是 16 的倍数），1k/2k 芯片决定尺寸档：
#   1:1 1024x1024 / 2048x2048    16:9 1024x576 / 2048x1152    9:16 576x1024 / 1152x2048
#   4:3 1024x768  / 2048x1536    3:4  768x1024 / 1536x2048    3:2  1008x672 / 2016x1344
#   2:3 672x1008  / 1344x2016
# 官方 gpt-image-1 只认三档尺寸，接官方时**不要**打开。
# OPENAI_IMAGE_FLEXIBLE_SIZES=1
# OPENAI_IMAGE_MODEL=gpt-image-2
# OPENAI_BASE_URL=https://ccgoai.club/v1

# 画质档 low / medium / high / auto，默认 high。请求里不显式带 quality 时上游按 medium 计费，
# 所以本项目每次都显式传。只在 OPENAI_IMAGE_FLEXIBLE_SIZES=1 时生效（官方路径仍是 1k→low / 2k→high）。
# OPENAI_IMAGE_QUALITY=high

# 图片价目表（quality × 尺寸档 × 张数，不按 token）。尺寸档按最长边：≤1024=1K、≤2048=2K、更大=4K。
# 设置后按档计价并忽略 usage token 精算；不设置则沿用官方口径（output_tokens × $40/M）。
# JSON 坏掉会记一条 warn 并自动回落到 token 口径，不会影响出图。
# ⚠️ 单位随上游而定：ccgoai 扣的是**人民币额度**，不是美元。一旦配置本表，
#    job.json 里的 costUsdEstimate / costUsdActual 就是「上游额度」而非 USD，做配额时别当美元读。
# 下面是 2026-09 实测的 ccgoai gpt-image-2 价目（元/张，1K 与 2K 同价）：
# OPENAI_IMAGE_PRICE_TABLE={"low":{"1K":0.08,"2K":0.08,"4K":0.10},"medium":{"1K":0.13,"2K":0.13,"4K":0.15},"high":{"1K":0.20,"2K":0.20,"4K":0.23}}
# 注意：出图常需 30–120 秒。生图路径走自己的 OPENAI_IMAGE_TIMEOUT_MS / OPENAI_IMAGE_TASK_TIMEOUT_MS
# （见文件末尾），不吃 UPSTREAM_TIMEOUT_MS，且永不重发已计费的生成请求。

# ── 可灵（Kling）直连视频（docs/plan-kling-video.md）─────────────────────────
#
# 可灵开放平台「新系统」的单串 API Key（控制台生成，不是旧版 AK/SK）。
# 缺失时可灵完全不参与路由；视频仍按 XAI_API_KEY / SUB2API_API_KEY 走 xAI。
# KLING_API_KEY=
# 域名不带 /v1——接口路径本身就是 /text-to-video/<model>、/tasks。
# 国内版（klingai.com/dev，人民币）用 api-beijing；国际版（美元充值，$10=100 积分）
# 的 key 只在 api-singapore 有效，在 beijing 会回 1002「api key not found」。
# KLING_BASE_URL=https://api-beijing.klingai.com
# KLING_BASE_URL=https://api-singapore.klingai.com
#
# 视频路由的显式开关：kling | grok，默认 grok。=kling 且有 key 时，
# **只有**文生视频 / 图生视频改走可灵；参考生视频 / 编辑 / 延长 / 30-60 秒长片仍留在 xAI
# （它们依赖 xAI 的 Files API 与一致性管线）。非法值回落 grok。
# 之所以要这条开关而不是「有 key 就用」：xAI key 会一直存在，不能被可灵一配上就抢走。
# VIDEO_PROVIDER=grok
#
# 模型名同时是 URL 路径段。kling-2.5-turbo 请求形状相同，可直接切。
# KLING_VIDEO_MODEL=kling-2.6
#
# 720p | 1080p，覆盖 UI 固定发的 720p。
# KLING_VIDEO_RESOLUTION=720p
#
# off | native。**有声只支持 1080p**，设为 native 时分辨率会被自动抬到 1080p 并写回任务记录
# （单价随之从 0.3 涨到 1.0 积分/秒）。UI 不暴露这个开关。
# KLING_VIDEO_AUDIO=off
#
# 积分 → USD 的换算率，只影响账目显示，默认 0.10（$10 = 100 积分的充值比例）。
# 提交时按「积分/秒 × 时长 × 本值」估价；实付以 poll 回来的 billing 为准（可灵是三家里
# 唯一给出真实扣费的上游）。
# KLING_USD_PER_UNIT=0.10
#
# 单个可灵任务的总时长上限（毫秒），与 runner 的 15 分钟轮询上限同口径。
# KLING_TASK_TIMEOUT_MS=900000
#
# ⚠️ 时长只有 5 / 10 两档（能力地图写的 3–10s 是营销口径，接口枚举只有这两个）。
#    服务端会把请求归一为「≤5 → 5、>5 → 10」并**写回 job.durationSec**——4 秒的请求
#    上游按 5 秒计费，账目必须如实。首页的时长芯片同时换成 5 / 10。

# 强制走 mock（有 key 也出 ffmpeg 水印片，便于 UI）
# LUMEN_FORCE_MOCK=1

# ── 用户系统（docs/plan-users-quota.md）─────────────────────────────────────
#
# 会话 Cookie（lumen_session）的 HMAC 密钥。**必填**：未设置或短于 16 字符时
# 服务启动即失败，不做任何静默降级。轮换它 = 让所有人立刻登出。
# 生成（PowerShell）：
#   -join ((1..32) | ForEach-Object { '{0:x2}' -f (Get-Random -Max 256) })
# LUMEN_SESSION_SECRET=
#
# 管理员的 user id（不是邮箱——邮箱可被抢注）。未设置时没有任何人是管理员，
# 迁移前留下的「无主任务」（job.json 里没有 ownerId）便对所有人不可见。
# 设置后它唯一的特权是：自己的任务列表里同时能看到这些无主任务。
# 该 id 由下面的部署顺序第 2 步产生。
# LUMEN_ADMIN_USER_ID=
#
# 部署顺序（plan §4，避免出现「谁都能注册」或「谁都进不去」的窗口）：
#   1. 先在 .env 写好 LUMEN_SESSION_SECRET → 重启到新版本。
#      此时 data/invites/ 为空，没人能注册——这是安全的默认。
#   2. 管理员生成 1 个邀请码并注册自己：
#        node scripts/mint-invites.mjs 1 --note "管理员"
#      把注册接口返回的 userId 写进 LUMEN_ADMIN_USER_ID → 再重启一次。
#   3. 之后按需 `node scripts/mint-invites.mjs 10 --note "第一批内测"` 分发。
#      码只打印到标准输出，不写日志；--note 用来记录发给了谁，便于追溯。
#   4. 旧的 LUMEN_ACCESS_TOKEN 已完全退役（proxy 只认会话 Cookie），从 .env 删掉即可。
#
# ── 定价 × 余额（方案 2026-09 §3.2，主闸门）───────────────────────────────
#
# 用户售价表（人民币元）。不设就用内置默认表：
#   {"video":{"5":2,"10":4,"hd":1.5,"audio":1},"extend":3,"edit":4,"image":{"1k":0.5,"2k":1}}
# video 的 "5" / "10" 是 ≤5 秒 / 更长两档基价，hd 是 1080p 的倍率，audio 是出声的加价；
# 30 / 45 / 60 秒长片按 5 秒段数 × "5" 计（30s = 12 元）。可以只写要改的几项，其余走默认。
# 这是**对用户的售价**，与上面 OPENAI_IMAGE_PRICE_TABLE（我们付给上游的成本）无关；
# 坏 JSON 只记一条 warn 并回落默认表，不会挡住提交。
# LUMEN_PRICE_TABLE={"video":{"5":2,"10":4,"hd":1.5,"audio":1},"extend":3,"edit":4,"image":{"1k":0.5,"2k":1}}
#
# 准入判据是「余额 − 在途任务预留 ≥ 本次售价」，不够返回 402 insufficient_balance；
# 成功的任务在终态那一刻扣款并往 data/ledger/<userId>.jsonl 记一行，失败 / 取消 / 过期
# 只释放预留、不扣钱。充值走 `node scripts/grant-balance.mjs <邮箱> <金额> --note "..."`。
# 新账号余额为 0，没有环境变量可以调这件事。
#
# ── 每日配额（plan §6，只对文生图计数；余额模型上线后退居防滥用兜底）────────
#
# 每人每天能出的图数，默认 200（余额是主闸门，这条只挡脚本刷图）。口径是「今日成功 + 当前在途 < 上限」：
# 提交即预留一个名额，成功落盘转为永久占用；上游故障、超时、用户取消都会
# 让任务转终态并自动释放预留，**不占额度**。「今日」按北京时间自然日算，
# 0 点重置（不做每用户时区）。超限返回 429 quota_exceeded。
# 管理员不豁免配额；迁移前的无主任务不计入任何人的额度。
# 设为 0 = 暂停所有人的生图提交。
# FREE_DAILY_IMAGE_QUOTA=200
#
# 止损阀：今日「失败 + 取消」达到它就暂停该账号的新提交（429 failure_limit_reached），
# 默认 30。它独立于配额，挡的是「反复提交再失败」持续消耗上游余额。
# FREE_DAILY_FAILURE_LIMIT=30

# 数据目录（默认 ./data）
# DATA_DIR=

# ── 数据留存（plan §8）─────────────────────────────────────────────────────
#
# 终态任务（成功 / 失败 / 取消 / 过期）完成超过这么多天后，删掉它的
# data/jobs/<id>/inputs/ 与 outputs/，只在 job.json 写 artifactsPurgedAt——
# 状态不变，记录仍在，作品环显示「作品已过期清理」的占位卡，且不再允许一键重试。
# 清理挂在任务 runner 的每小时定时器上（启动跑一次）。
# data/tmp/ 与 data/idempotency/ 里超过 24 小时的文件同时清掉，与本项无关、不可关。
# 0 = 关闭清理，永久保留。默认 30。
# DATA_RETENTION_DAYS=30

JOB_CONCURRENCY=2
MAX_QUEUED_JOBS=20
UPSTREAM_TIMEOUT_MS=30000
UPSTREAM_RETRY_BASE_MS=250

# 一致性管线（30 / 45 / 60 秒长片）。M2.4 起生效：开启后 UI 多出长片时长，
# 任务走 Director → Keyframe → Shots(+QC) → Stitch。mock 模式可放心开启；真实 key 下
# 单片成本 ≈ $2.1–4.2，超过预估 ×2 自动停止重试。
HARNESS_ENABLED=false
# 每个长片任务并行生成的 shot 数（1–4）
# HARNESS_SHOT_CONCURRENCY=2
# grok-4.6 视觉一致性 QC 阈值（0–1）。不设即跳过视觉打分——阈值需用 evals/runs 对照集校准后再固定。
# HARNESS_QC_VISUAL_THRESHOLD=0.6
WORKFLOWS_ENABLED=false
JIMENG_ENABLED=false

# 生图的两条超时，管的不是同一件事，别混：
#
# 1) OPENAI_IMAGE_TIMEOUT_MS —— **单次 HTTP 请求**的超时（毫秒），默认 180000，上限 10 分钟。
#    生成 POST 与取结果 GET 都用它。生成 POST 不重试：超时即失败，由用户决定是否重新提交
#    （每个被上游接受的生成请求都计费，自动重试就是重复付钱）。
# OPENAI_IMAGE_TIMEOUT_MS=180000
#
# 2) OPENAI_IMAGE_TASK_TIMEOUT_MS —— **整个异步出图任务**的总上限（毫秒），默认 600000（10 分钟），
#    上限 60 分钟。中转站（ccgoai）出图慢时不返回图片，而是回 HTTP 202 + 任务 id，本地按响应里的
#    poll_after_ms（默认 2000，下限 1s、上限 30s）轮询 GET /v1/images/tasks/{id}，成功后再
#    GET /v1/images/tasks/{id}/result 取二进制。实测 high 档 2K 一张约 102 秒，4K 更久，所以
#    这条要远宽于单请求超时。超时只是本地放弃等待：任务在上游仍活着，也仍然只在取回 result 时才计费。
# OPENAI_IMAGE_TASK_TIMEOUT_MS=600000

# smoke 脚本（scripts/smoke-lumen.mjs / smoke-cancel.mjs）用的登录账号。
# /api/* 全部需要会话，脚本先 POST /api/auth/login 拿 lumen_session Cookie 再跑用例。
# 账号不存在且给了 LUMEN_SMOKE_INVITE 时会先用该一次性邀请码注册。凭据只在本机环境，不进日志。
# LUMEN_SMOKE_EMAIL=
# LUMEN_SMOKE_PASSWORD=
# LUMEN_SMOKE_INVITE=
# 目标实例（默认 http://localhost:3000；Next 16 dev 对 127.0.0.1 回 403，别用它）
# LUMEN_URL=
